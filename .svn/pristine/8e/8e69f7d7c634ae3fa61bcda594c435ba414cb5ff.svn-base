var app = angular.module('myApp');
app.controller('maxSeatAlertCtrl', function ($scope ,items ,$modalInstance) {
	
	$scope.init = function(){ 
		$scope.message = items;
	}
	
	$scope.close = function () {
		    $modalInstance.dismiss('');
		   // window.location.href = window.location.href.replace(/#.*$/, '');
		};
		
	$scope.init();
});